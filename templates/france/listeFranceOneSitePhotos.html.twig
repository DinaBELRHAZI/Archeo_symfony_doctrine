<!DOCTYPE html>
<html >
<head>

    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <link href="{{asset('css/listeFranceOneSite.css')}}" rel="stylesheet" media="all">
</head>
<body>



<div class='header'>
    <p class='text-center'>{{ oneSite.getNomdusite}}</p>
    <strong>
        <a class="btn btn-outline-primary" id="btn-liste" href='{{ path("listAllFrance") }}' >
            Liste des sites
        </a>
    </strong>
    <strong>
        <a class="btn btn-outline-success" id="btn-carte" href='{{ path("listeFranceConvert") }}' >
            Carte
        </a>
    </strong>
    <strong>
        <a class="btn btn-outline-info" id="btn-details" href='/france/liste/site/{{ oneSite.id}}' >
            Plus de détails
        </a>
    </strong>
    <strong>
        <a class="btn btn-outline-warning" id="btn-ajout-photos" href='/france/liste/site/{{ oneSite.id}}/photos/ajout' >
            Ajouter des photos
        </a>
    </strong>
</div>

<div class='container-photos'>


    <div id="demo" class="carousel slide" data-ride="carousel">

        <!-- Indicators -->
        <ul class="carousel-indicators ">

            {% for i in 0..images|length %}

                {% if i == 0 %}
                    <li data-target="#demo" data-slide-to="{{ i }}" class="active" ></li>
                {% else %}
                    <li data-target="#demo" data-slide-to="{{ i }}"  ></li>
                {% endif %}


            {% endfor %}
        </ul>




        <!-- The slideshow -->
        <div class="carousel-inner">
            {% set i = 0 %}
            {% for img in images %}
                {% if i == 0 %}
                    <div class="carousel-item active">

                {% else %}
                    <div class="carousel-item ">
                {% endif %}

                <img src="{{asset('uploads/')}}{{ img.getName()}}" class="imgCar" >
                {% set i = i + 1 %}
            </div>

            {% endfor %}
        </div>


        <!-- Left and right controls -->
        <a class="carousel-control-prev" href="#demo" data-slide="prev">
            <span class="carousel-control-prev-icon"></span>
        </a>
        <a class="carousel-control-next" href="#demo" data-slide="next">
            <span class="carousel-control-next-icon"></span>
        </a>


    </div>


</div>

</body>
</html>

 