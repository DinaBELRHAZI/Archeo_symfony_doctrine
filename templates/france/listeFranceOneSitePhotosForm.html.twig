<!DOCTYPE html>
<html lang="fr">

<head>
    <!-- Required meta tags-->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">


    <!-- Icons font CSS-->
    <link href="{{asset('vendor/mdi-font/css/material-design-iconic-font.min.css')}}" rel="stylesheet" media="all">
    <link href="{{asset('vendor/font-awesome-4.7/css/font-awesome.min.css')}}" rel="stylesheet" media="all">
    <!-- Font special for pages-->
    <link href="https://fonts.googleapis.com/css?family=Poppins:100,100i,200,200i,300,300i,400,400i,500,500i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">

    <!-- Vendor CSS-->
    <link href="{{asset('vendor/select2/select2.min.css" rel="stylesheet')}}" media="all">
    <link href="{{asset('vendor/datepicker/daterangepicker.css')}}" rel="stylesheet" media="all">

    <!-- Main CSS-->
    <link href="{{asset('css/create_form.css')}}" rel="stylesheet" media="all">
</head>

{%  block content %}

    <div class="page-wrapper bg-gra-01 p-t-20 p-b-100 font-poppins">
        <div class="wrapper wrapper--w780">
            <div class="card card-3">
                <div class="card-heading"></div>
                <div class="card-body">
                    <h2 class="title">Ajouter une photo au site : {{ oneSite.getNomdusite()}}</h2>
                    <!-- Déclaration du form-->
                    <form name="monform" action="{{ path('OneSitePhotosAdd') }}" method="POST" enctype="multipart/form-data">

                            <input type="hidden" name="id_france" id="id_france" value="{{ oneSite.id}}" >
                            <div class="input-group">
                                <label>Veuillez choisir une image :</label>
                                <input class="input--style-3" type="file" name="name" id="name" value="" >
                            </div>
                            <div class="p-t-10">
                                <button class="btn btn--green btn--radius btn--pill" type="submit">Envoyer</button>
                            </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Jquery JS-->
    <script src="../../vendor/jquery/jquery.min.js"></script>
    <!-- Vendor JS-->
    <script src="../../vendor/select2/select2.min.js"></script>
    <script src="../../vendor/datepicker/moment.min.js"></script>
    <script src="../../vendor/datepicker/daterangepicker.js"></script>

    <!-- Main JS-->
    <script src="{{asset('js/create_form.js')}}"></script>

{%  endblock  %}